{
  "version": "0.0.8",
  "scripts": {
    "version:all:patch": "yarn workspaces foreach -p --no-private version patch",
    "version:all:minor": "yarn workspaces foreach -p --no-private version minor",
    "version:all:major": "yarn workspaces foreach -p --no-private version major"
  },
  "packageManager": "yarn@3.1.1",
  "workspaces": [
    "packages/**"
  ],
  "devDependencies": {
    "typescript": "^4.5.5",
    "vite": "^2.8.0"
  }
}
